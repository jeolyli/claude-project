{
  "name": "lingji-frontend-vanilla",
  "version": "1.0.0",
  "private": true,
  "description": "灵记 (LingJi) — 前端 Vanilla JS 工程 · 测试配置",
  "type": "module",
  "scripts": {
    "test": "vitest run --config vitest.config.js",
    "test:watch": "vitest --config vitest.config.js",
    "test:coverage": "vitest run --config vitest.config.js --coverage"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "jsdom": "^26.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "@vitest/ui": "^3.0.0"
  }
}
